name: Publish Cloud.ru image

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  image:
    runs-on: ubuntu-latest
    env:
      CLOUD_REGISTRY_HOST: ${{ secrets.CLOUD_REGISTRY_HOST }}
      CLOUD_REGISTRY_KEY_ID: ${{ secrets.CLOUD_REGISTRY_KEY_ID }}
      CLOUD_REGISTRY_KEY_SECRET: ${{ secrets.CLOUD_REGISTRY_KEY_SECRET }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          platforms: linux/amd64
          tags: |
            ghcr.io/vsnp-it-msk/vsnp-pomosh-max:latest
            ghcr.io/vsnp-it-msk/vsnp-pomosh-max:${{ github.sha }}

      - name: Log in to Cloud.ru Artifact Registry
        if: ${{ env.CLOUD_REGISTRY_HOST != '' && env.CLOUD_REGISTRY_KEY_ID != '' && env.CLOUD_REGISTRY_KEY_SECRET != '' }}
        uses: docker/login-action@v3
        with:
          registry: ${{ env.CLOUD_REGISTRY_HOST }}
          username: ${{ env.CLOUD_REGISTRY_KEY_ID }}
          password: ${{ env.CLOUD_REGISTRY_KEY_SECRET }}

      - name: Push image to Cloud.ru Artifact Registry
        if: ${{ env.CLOUD_REGISTRY_HOST != '' && env.CLOUD_REGISTRY_KEY_ID != '' && env.CLOUD_REGISTRY_KEY_SECRET != '' }}
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          platforms: linux/amd64
          tags: |
            ${{ env.CLOUD_REGISTRY_HOST }}/vsnp-pomosh-max:latest
            ${{ env.CLOUD_REGISTRY_HOST }}/vsnp-pomosh-max:${{ github.sha }}
